# 蒙特卡洛流体仿真 GPU 加速方案

## 1. 现状分析

### 1.1 当前实现

两种方法均已在 RTX 5060 Laptop GPU（8GB, CUDA 13.3）上运行：

| 方法 | 实现 | 加速手段 |
|------|------|---------|
| 2022 涡量法 | CuPy (Python) | Biot-Savart 向量化（较 CPU 提速约 120 倍）|
| 2024 速度法 | OptiX (C++) | 官方 GPU 实现（含 WoB 光线追踪投影）|

### 1.2 瓶颈定位（2022 涡量法）

对 2022 涡量法进行剖析，单步计算时间分布：

| 子步骤 | 占比 | 问题 |
|--------|------|------|
| Biot-Savart 速度重建 | ~50% | 创建 5 个中间数组（dxm,dym,r2,bx,by），内存带宽受限 |
| 半拉格朗日平流 + 插值 | ~50% | 使用 CuPy 高级索引 + cp.fuse，产生多个 kernel 与中间数组 |

以 128×128、nmc=256 为例，Biot-Savart 每个中间数组占用 16.8MB，5 个即 84MB 内存流量。

## 2. 加速方案

### 方案 A：定制融合 CUDA Kernel（已实现，收益最大）

用 CuPy RawKernel 编写融合核函数，将 Biot-Savart 的核函数计算 + 采样插值 + 累加**合并为单个 kernel**，每个查询点一个线程：

- **彻底消除中间数组**：84MB 内存流量 → 0
- **避免 kernel 启动开销**：5 个 kernel → 1 个
- **实测加速**：

| 网格 | 原版 (ms/步) | 优化版 (ms/步) | 加速比 |
|------|-------------|---------------|--------|
| 64×64 | 1.73 | 0.18 | **9.6×** |
| 128×128 | 1.83 | 0.26 | **6.9×** |
| 256×256 | 7.44 | 0.47 | **15.8×** |

数值验证与优化前一致（float32 精度内）。

### 方案 B：CUDA Graph 捕获整个时间步

将整个 `step()`（Biot-Savart + 平流 + 扩散）捕获为 CUDA Graph，消除所有 kernel 启动延迟。对短 kernel 密集的调用（当前每步约 10-20 个 kernel）收益明显，预期再加速 1.5-3×。

### 方案 C：共享内存优化

- 采样点数据（nmc 个 float2）加载到**共享内存**，块内所有线程复用，减少全局内存访问；
- 涡量网格分块存入共享内存，插值命中率提升。

### 方案 D：网格点分块 + 并行归约

- Biot-Savart 每查询点对 N 个采样累加，可拆分为多线程部分归约，用 warp/block 归约加速；
- 对大网格（256² 以上）收益明显。

### 方案 E：Numba CUDA 备选

若需更灵活的 kernel 定制（如向量化 SIMD、__ldg 只读缓存），可用 Numba CUDA 替代 CuPy RawKernel。

## 3. 已实现成果

`gpu_fluid_2022_opt.py` 已实现方案 A（融合 Biot-Savart + 融合插值 kernel）：

```python
from gpu_fluid_2022_opt import Sim2022GPUOpt
sim = Sim2022GPUOpt(128, 128, dt=0.05, nmc=256)
```

对比优化前（`gpu_fluid_2022.py`）：
- 单步耗时降低 7-16 倍
- 内存流量降低约一个数量级
- 数值结果一致

## 4. 2024 速度法（OptiX）的加速空间

2024 方法已是官方 OptiX 实现，进一步的 GPU 优化方向：

| 方向 | 说明 |
|------|------|
| VPL 路径缓存 | 在不同评估点间复用 WoB 边界路径（论文已实现，可调优参数）|
| 增大 num_path_samples | 更高采样质量但线性增成本（误差-成本 Pareto 权衡）|
| 批量场景切换 | 论文的多场景并行 |
| CUDA 图 | 与方案 B 同理，降低 OptiX 启动开销 |

## 5. 预期总结

| 方案 | 实现难度 | 预期加速 | 状态 |
|------|---------|---------|------|
| A. 融合 Kernel | 中 | 7-16× | ✅ 已实现 |
| B. CUDA Graph | 低 | 1.5-3× | 待实现 |
| C. 共享内存 | 中 | 1.5-2× | 待实现 |
| D. 并行归约 | 中 | 2-4×（大网格）| 待实现 |

综合方案 A-D 后，2022 涡量法相对当前 CPU 版的总加速预计可达 **10³ 量级**（CPU 80.9s → GPU 毫秒级）。
